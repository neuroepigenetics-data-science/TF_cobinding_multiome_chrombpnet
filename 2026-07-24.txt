================================================================================
WORK SUMMARY — 2026-07-24
Zamboni et al. multiome pipeline: de-hardcoding refactor + data + environment
================================================================================

HEADLINE
--------
All 4 preprocessing scripts are now refactored (config-driven), ALL per-sample
data is local and verified, and the R packages are installed EXCEPT ArchR, whose
install was still running when we stopped. Nothing has been run end-to-end yet.

Run order (archr is FIRST — it feeds create_object):
  archr_peak_calling.R -> create_object.R -> filtering.R -> merge_sample_objects.R


WHAT GOT DONE TODAY
-------------------
1. Refactored the remaining 3 scripts into pipeline/R/ (create_object was already
   done). All config-driven via pipeline/config.yaml + pipeline/samples.csv:
     - pipeline/R/filtering.R
     - pipeline/R/merge_sample_objects.R
     - pipeline/R/archr_peak_calling.R
   Each documents deliberate departures from the published code in its header.
   Notable fixes vs the paper's scripts:
     * filtering.R: per-sample best_pK now read from samples.csv (paper had a
       best_pK<-best_pK[i] aliasing bug -> pK=NA for all but sample 1); peak-assay
       names (macs2_peaks/cellranger_peaks) mapped to the real "peaks" assay;
       manual QC keyed by sample_id not positional [[18]]..[[21]]; DoubletFinder
       2.0.6 API (doubletFinder(), reuse.pANN=NULL).
     * merge_sample_objects.R: experiment/condition/batch from samples.csv (not
       strsplit); computes all cluster_resolutions incl. the wsnn_res.0.1 that the
       annotation uses (paper only ran res 0.2 -> that column never existed);
       RNA-only UMAP from pca, ATAC-only from lsi (paper omitted reduction= -> both
       defaulted to pca). *** cluster->celltype map is DATA-SPECIFIC. ***
     * archr_peak_calling.R: new valid_barcodes() helper reads per_barcode_metrics
       (both CR layouts, via config barcode_metrics); macs2 path from config.
   All three parse cleanly; config loads; the tricky outlier-detection rewrite in
   filtering.R was unit-tested byte-identical to the original.

2. Fetched all remaining per-sample DATA from Zamboni's Dropbox (tokens from the
   earlier download_fragments.sh were STILL LIVE 3 days later; only the preview=
   filename changed). Now under data/<SAMPLE>/outs/ for all 21 samples:
     - per_barcode_metrics.csv        (95,672 pre-QC called cells total; verified)
     - cellbended_gex_matrix_fpr001_seurat.h5   (valid HDF5)
     - filtered_feature_bc_matrix/{matrix,barcodes,features}.gz  (Read10X ok:
       returns "Gene Expression | Peaks")
   Download scripts: /private/tmp scratchpad (download_barcode_metrics.sh,
   download_gex_inputs.sh). GOTCHA: filtered_feature_bc_matrix is a SUBDIR — the
   token is bound to the outs/ path, so use preview=filtered_feature_bc_matrix%2F<file>
   (URL-encoded slash), NOT a deeper path (that returns HTML).

3. Installed R/tools into conda env zamboni-r (run: conda run -n zamboni-r Rscript ...):
     - hdf5r 1.3.12         (conda-forge)  -> Read10X_h5 works
     - DoubletFinder 2.0.6  (github)       -> NEW API; filtering.R already updated
     - macs2 2.2.9.1        -> SEPARATE conda env "macs2" (zamboni-r has python 3.14
       which macs2 can't use). Path wired into config.yaml archr.macs2_path:
       /Users/gorkemdurmaz/miniconda3/envs/macs2/bin/macs2
     - devtools + ArchR 1.0.3: **INSTALLED & VERIFIED.** (First ArchR attempt
       failed on a missing 'devtools' dep — the exit-0 was misleading; fixed by
       installing r-devtools via conda-forge and retrying. library(ArchR) loads.)
   => ALL packages now installed. code/source.R loads end-to-end. Every script
      is runnable. NOTHING has been run as a pipeline yet.


>>> RESUME HERE ON MONDAY <<<
----------------------------
1. (Optional sanity check — should already be TRUE:)
     conda run -n zamboni-r Rscript -e 'cat(requireNamespace("ArchR"),"\n")'
   All packages + all data are in place; skip straight to the smoke-test.

2. Smoke-test the chain. The first real run is archr_peak_calling.R and it's
   HEAVY (21 samples + MACS2 peak calling). Recommend a 2-sample subset first:
   copy pipeline/samples.csv to a test sheet with 2 rows, point config.project.samples
   at it, run archr -> create_object -> filtering -> merge, then compare to
   Zamboni's multiome_obj_clean_240326.rds (repo root).

3. Deferred task: add Snakemake orchestration over the 4 scripts.


KEY FILES
---------
  pipeline/config.yaml        - all params (added: assays, barcode_metrics, archr,
                                expanded filtering + merge blocks, archr.macs2_path)
  pipeline/samples.csv        - 21 samples (sample_id, path, experiment, condition,
                                batch, best_pK)
  pipeline/R/pipeline_utils.R - loaders + helpers (outs_path, valid_barcodes,
                                fix_gene_names)
  pipeline/R/*.R              - the 4 refactored steps
  pipeline/README.md          - status table + env table + "Where to resume"
  code/                       - ORIGINAL scripts, untouched (reference)
  data/<SAMPLE>/outs/         - all per-sample inputs (local, verified)
  multiome_obj_clean_240326.rds - Zamboni's merged annotated object (ground truth)
================================================================================
